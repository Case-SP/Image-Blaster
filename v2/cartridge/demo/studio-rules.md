# Studio rules — demo cartridge

Single composition: `contact-sheet-2x3`. Single subject: `panel-mix`. Single theme: `fafafa-flat`.

The shot-list LLM should pick this composition for every shot. The variety lives in the panel slots (`p1`–`p6` in compositions.json), which the user populates with the brand brief.
