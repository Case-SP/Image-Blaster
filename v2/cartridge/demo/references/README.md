Drop 5–6 reference JPG/PNG/WEBP files here. Filenames don't matter; the loader auto-picks the first 8 (`v2/src/factory/cartridge.js:30-39`).

Note: only `fal-ai/nano-banana-pro` and `fal-ai/flux-pro/kontext` actually use references at render time. The cheap `fal-ai/nano-banana` variant ignores them, which is fine for the cost-optimized blast.
